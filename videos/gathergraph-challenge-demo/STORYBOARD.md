---
format: 1920x1080
duration: 76.544s
message: "WebMCP lets an agent coordinate independent providers while the human keeps visible control over repair, approval, and commitment"
arc: Problem → Native contract → One request → Visible repair → Human boundary → Durable evidence
audience: WebMCP Challenge judges, browser-agent builders, and product teams
mode: autonomous
music: none
captions: enabled
---

# GatherGraph Challenge Demo

## Video direction

Use the real hosted GatherGraph capture as the product surface, framed with the warm civic-editorial system in `frame.md`. Keep the page recognisable and legible: cream canvas, ink type, restrained blue structure, amber decision accents. The motion language is measured and evidential—short camera pushes, card assembly, cursorless state changes, and ledger receipts—never synthetic sci-fi glow. Captions occupy the bottom 17% keep-out band. Use clean cuts or restrained crossfades between chapters; do not imply that any booking, payment, message, or supplier contact is real.

The detailed beat ranges below preserve the original long-form direction. The
frame `duration` fields and assembled composition are authoritative for the
current 76.544-second narrated cut.

## Frame 1 — The coordination problem

- status: animated
- src: compositions/frames/01-coordination-problem.html
- duration: 12.48s
- transition_in: cut
- poster: 13s
- scene: One catered event is fragmented across venue, catering, and access/setup websites; GatherGraph introduces structured provider contracts.
- voiceover: "You describe the catered event once, then stay in your chat. GatherGraph lets your agent coordinate the venue, caterer, and setup across separate websites without asking you to search, compare, or copy the details yourself."
- blueprint: compose
- focal: assets/gathergraph-hero.png
- asset_candidates: assets/gathergraph-hero.png, assets/gathergraph-tools.png
- roles: gathergraph-hero background product surface; gathergraph-tools supporting native-contract evidence

**0.0–4.5s — Scene 1 · the human outcome.** Full-bleed `gathergraph-hero.png` enters already visible inside a shallow browser frame. Layout: product surface fills the upper 78%, with one concise label, `ONE GATHERING`, anchored high-left. Motion: a restrained `gsap-effects` opacity settle and slow `multi-phase-camera` push toward the example request.

**4.5–11.5s — Scene 2 · three disconnected jobs.** Instantiate the `comparison-split` signature as two mirrored evidence cards over the real page: venue on the left; catering + access/setup paired on the right. They arrive from opposite wings with shallow mirrored `rotateY`, then a third access chip lands between them to break the false two-site simplicity. Visible copy is limited to `VENUE`, `CATERING`, `ACCESS & SETUP`. Motion: `split-tilt-cards`, one restrained `spring-pop-entrance`, and subtle phase-opposed `sine-wave-loop`.

**11.5–18.0s — Scene 3 · contract, not guessed clicks.** The cards settle into three aligned provider strips while `STRUCTURED CONTRACTS` appears above them. Bring in a cropped region of `gathergraph-tools.png` behind the strips as evidence, not as a rebuilt interface. Motion: `discrete-text-sequence`, short direct-to-slot `center-outward-expansion`, and a gentle camera push that ends with the real product dominant.

## Frame 2 — Why native WebMCP matters

- status: animated
- src: compositions/frames/02-native-contract.html
- duration: 14.101s
- transition_in: crossfade
- poster: 15s
- scene: Four parent orchestration tools coordinate venue, catering, and caterer-access documents with three tools each—thirteen tools across four document contexts.
- voiceover: "The venue, catering, and caterer-access services are independent same-origin documents. Each owns three WebMCP tools. GatherGraph owns four more for composition, conflict repair, approval, and the final simulated dossier."
- blueprint: compose
- focal: assets/gathergraph-tools.png
- asset_candidates: assets/gathergraph-tools.png, assets/gathergraph-full-page.png
- roles: gathergraph-tools foreground proof surface; gathergraph-full-page supporting whole-product context

**0.0–5.0s — Scene 1 · show the native proof.** Open on the real `gathergraph-tools.png` crop at readable scale with `NATIVE WEBMCP` as the only editorial label. Reveal it with a short opacity settle and a steady camera push into the ownership area; the product surface is visible by 0.2s.

**5.0–13.5s — Scene 2 · ownership assembles.** Over the real screenshot, assemble four labelled groups into a 2×2 evidence grid: `PARENT · 4`, `VENUE · 3`, `CATERING · 3`, `ACCESS / SETUP · 3`. Each group travels only a short distance into its slot using `center-outward-expansion`; stagger the labels and let one restrained light wash pass behind the completed grid.

**13.5–20.0s — Scene 3 · make the arithmetic explicit.** Resolve the grid into the concise proof line `4 + 3 + 3 + 3 = 13`, with the sublabel `FOUR DOCUMENT CONTEXTS`. Keep the underlying native screenshot visible and unaltered. Use `counting-dynamic-scale` for the total, then hold quietly with only a slight drift on the four group markers.

## Frame 3 — One human request

- status: animated
- src: compositions/frames/03-one-request.html
- duration: 10.261s
- transition_in: crossfade
- poster: 21s
- scene: A person states an accessible dinner outcome; the agent gathers structured capacity, menu, route, timing, allergen, and footprint evidence.
- voiceover: "The person describes the outcome, not the clicks. The agent gathers structured capacity, accessibility, menu, allergen, route, timing, and footprint evidence from the right provider."
- blueprint: compose
- focal: assets/gathergraph-workspace.png
- asset_candidates: assets/gathergraph-workspace.png, assets/gathergraph-passport.png
- roles: gathergraph-workspace background real planning surface; gathergraph-passport supporting authority context

**0.0–7.0s — Scene 1 · outcome, not clicks.** Use `gathergraph-workspace.png` full-bleed inside a browser frame. A compact prompt composer overlays the measured request region and types `Accessible dinner · 40 people · under £1,800`. Motion: the `prompt-type-submit-generate` type-and-submit signature using `discrete-text-sequence`; no cursor choreography and no narration sentence duplicated on screen.

**7.0–18.0s — Scene 2 · providers answer in structure.** After submit, three provider strips populate in sequence on top of their corresponding areas of the real screenshot. Short evidence pills appear: `CAPACITY 40`, `STEP-FREE`, `ALLERGENS`, `17:15`, `ROUTE`, `FOOTPRINT`. Motion: `stat-bars-and-fills`, direct-to-slot `center-outward-expansion`, and `discrete-text-sequence` state swaps paced across the voiceover.

**18.0–27.0s — Scene 3 · receipts, not a magic answer.** A narrow receipt rail assembles at the right edge with three check-marked provider receipts, while the camera eases back to show the complete shared workspace. Motion: `svg-path-draw`, low-drama `gsap-effects` stagger, and `multi-phase-camera` pull-back. Finish on `3 PROVIDERS · 1 VISIBLE PLAN`.

## Frame 4 — Conflict and repair stay visible

- status: animated
- src: compositions/frames/04-visible-repair.html
- duration: 15.232s
- transition_in: cut
- poster: 20s
- scene: GatherGraph exposes a caterer-arrival-before-access conflict, moves setup to 17:15, and records exactly what changed.
- voiceover: "The first plan is not silently presented as correct. The caterer proposes arriving at 16:45, but the venue does not open to suppliers until 17:00. The agent moves setup to 17:15 and records exactly what changed."
- blueprint: compose
- focal: assets/gathergraph-workspace.png
- asset_candidates: assets/gathergraph-workspace.png, assets/gathergraph-ledger.png
- roles: gathergraph-workspace background conflict surface; gathergraph-ledger foreground repair receipt

**0.0–7.5s — Scene 1 · fail visibly.** Open on the conflict area of `gathergraph-workspace.png` at readable scale. Highlight, without replacing, the real `Caterer arrival at 16:45 conflicts` state. Use a restrained amber attention wash and a short coordinate push.

**7.5–17.0s — Scene 2 · edit and synchronise.** The left panel shows `16:45` changing to `17:15`; the right ledger panel updates in response. A thin connector line draws between the changed field and its receipt. Use `discrete-text-sequence` for the time change and `svg-path-draw` for the connector, followed by one restrained repaired-status settle.

**17.0–25.0s — Scene 3 · preserve the delta.** Bring `gathergraph-ledger.png` forward as the real evidence surface and stagger in `BEFORE 16:45` / `AFTER 17:15` / `REASON access window`. Hold nearly still; end on `REPAIRED ≠ APPROVED` to set up the next boundary.

## Frame 5 — The human approval boundary

- status: animated
- src: compositions/frames/05-human-boundary.html
- duration: 14.976s
- transition_in: crossfade
- poster: 22s
- scene: Repair is separated from approval, and approval is separated from the simulated browser-local dossier commitment.
- voiceover: "Only now does the agent come back to the person. One final yes or no is the only required human action. Without approval, dossier creation fails closed. With it, the demo assembles a browser-local dossier—no booking, payment, or message is sent."
- blueprint: compose
- focal: assets/gathergraph-passport.png
- asset_candidates: assets/gathergraph-passport.png, assets/gathergraph-workspace.png, assets/gathergraph-ledger.png
- roles: gathergraph-passport foreground authority surface; gathergraph-workspace background plan context; gathergraph-ledger supporting receipt proof

**0.0–7.5s — Scene 1 · separate the gates.** The real `gathergraph-passport.png` sits in a clean browser/device frame over a quiet cream field. Three gate labels assemble vertically: `REPAIR`, `APPROVAL`, `COMMITMENT`, with only the first marked complete. Motion: `grid-card-assemble` vertical-list variant, `svg-path-draw`, and a slow `multi-phase-camera` push.

**7.5–18.5s — Scene 2 · stop for a person.** A visible human-approval panel rises from the real product surface and holds at centre. Attempted dossier creation appears as `BLOCKED · APPROVAL RECEIPT REQUIRED`; after a deliberate beat the approval receipt lands. Motion: `device-surface-showcase` staged surface reveal, `discrete-text-sequence`, and one restrained `spring-pop-entrance` on the receipt.

**18.5–28.0s — Scene 3 · simulated and browser-local.** The approved state reveals a dossier card beside the ledger crop. Four safety chips arrive across the remaining narration: `NO BOOKING`, `NO PAYMENT`, `NO MESSAGE`, `BROWSER-LOCAL`. Motion: direct-to-slot `center-outward-expansion`, `svg-path-draw` checks, then a quiet evidence hold.

## Frame 6 — Shared control, durable evidence

- status: animated
- src: compositions/frames/06-shared-control.html
- duration: 17.024s
- transition_in: crossfade
- poster: 21s
- scene: The person normally remains in chat; an optional audit view exposes every supplier check and receipt while preserving human control.
- voiceover: "The person normally stays in chat and sees the final plan. This optional audit view shows every supplier check and receipt, and lets the person take over manually at any time. GatherGraph: one human brief, every supplier coordinated by an agent, and one final decision that stays with you."
- blueprint: compose
- focal: assets/gathergraph-full-page.png
- asset_candidates: assets/gathergraph-full-page.png, assets/gathergraph-ledger.png, assets/gathergraph-hero.png
- roles: gathergraph-full-page background full product journey; gathergraph-ledger foreground evidence rail; gathergraph-hero supporting final lockup

**0.0–9.0s — Scene 1 · one shared surface.** Travel slowly down `gathergraph-full-page.png` inside a clipped browser viewport, beginning around the brief and workspace. Overlay two compact markers—`HUMAN` and `AGENT`—on the same plan rather than in separate worlds. Use a seek-safe `viewport-change` vertical pan and subtle static brackets around the current constraint field.

**9.0–19.0s — Scene 2 · artifact from the trail.** The journey resolves into the real `gathergraph-ledger.png`, where receipt rows assemble as the durable artifact. Use `discrete-text-sequence` for the receipt states and short direct-to-slot list motion. Land the concise proof `VISIBLE · REVERSIBLE · RECEIPTED`.

**19.0–27.0s — Scene 3 · close on the product.** Crossfade to `gathergraph-hero.png`, settle the GatherGraph name at readable scale, and arrange three small provider markers beneath it around one human-control mark. Use a restrained light wash, short opacity settle, and a final slow camera pull-back. Hold long enough to read `ONE HUMAN BRIEF · EVERY SUPPLIER COORDINATED · ONE FINAL DECISION`.
